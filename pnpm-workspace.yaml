packages:
  - 'packages/*'
  - 'apps/*'

# pnpm 10+ 把这些设置迁到了 pnpm-workspace.yaml（package.json 的 "pnpm" 字段已不再读取）
# esbuild 是 tsx / vite 的依赖，需要允许其安装脚本运行；否则 tsx 与 vitest 可能报缺二进制
allowBuilds:
  esbuild: true
